export type FoodConfig = {
  filter: boolean;
  random: boolean;
  search: boolean;
  searchInput?: string;
};

export type FilterConfig = {
  effort: string;
  deliverable: string;
  cheeseometer: string;
  tags: string;
};
