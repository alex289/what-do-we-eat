export type FoodConfig = {
  filter: boolean;
  random: boolean;
  search: boolean;
  searchInput?: string;
};

export type FilterConfig = {
  size: string;
  effort: string;
  deliverable: string;
  cheeseometer: string;
};
